import { Helmet } from "react-helmet";

const NotFoundPage = () => {
  return (
    <div>
      <Helmet>
        <title>NotFoundPage</title>
      </Helmet>
      NotFoundPage
    </div>
  );
};

export default NotFoundPage;
